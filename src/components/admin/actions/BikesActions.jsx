import { Grid, Typography } from "@mui/material";

const BikesActions = () => {
    return (
        <Typography>
            Bike Add / Remove / Edit(move, send to service)
        </Typography>
    )
}

export default BikesActions;
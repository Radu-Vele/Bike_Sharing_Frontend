import React from "react";


const Footer = (props) => {
  return (
    <footer className={props.class}>
      &copy; Add important details here @contact
    </footer>
  );
};

export default Footer;
